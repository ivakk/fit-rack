# Prometheus scrape (Docker internal network only — not exposed via Traefik)
management.endpoints.web.exposure.include=health,prometheus
management.endpoint.health.show-details=when_authorized
management.metrics.tags.application=iam-service
