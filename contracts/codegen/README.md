# Contract codegen

Scripts and generated output for TypeScript and Python clients derived from `contracts/asyncapi/`.

Generated artifacts should typically be gitignored under `out/` until you decide to vendor them in CI.
